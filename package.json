{
  "name": "instagram_directory_api",
  "version": "1.0.0",
  "description": "An Instagram Stores Directory API",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon server.js",
    "precommit": "lint-staged",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "lint-staged": {
    "*.{js,jsx}": [
      "prettier --write --no-semi --single-quote --trailing-comma all --print-width 100",
      "git add"
    ]
  },
  "author": "@anamariasosam",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.4",
    "dotenv": "^4.0.0",
    "express": "^4.16.2",
    "mongoose": "^4.13.5"
  },
  "devDependencies": {
    "husky": "^0.14.3",
    "lint-staged": "^5.0.0",
    "nodemon": "^1.12.1",
    "prettier": "^1.8.2"
  }
}
